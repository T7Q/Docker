docker inspect -f '{{.Config.Env}}' spawning-pool



# ------------------------------------------------------------------------------
# For running container, this command is also possible << docker exec spawning-pool env >>
# ------------------------------------------------------------------------------
