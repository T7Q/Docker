docker run -d --name overlord -p 5000:80 --restart=always nginx

# -d		     * detached: docker container runs in the background of your terminal. 
# --name	     * name container https://docs.docker.com/engine/reference/run/
# -p 5000:80     * map port 80 in the container to port 5000 of the Dockere host (Char) https://docs.docker.com/config/containers/container-networking/
# restart=always * always restart the container if it stops https://docs.docker.com/config/containers/start-containers-automatically/
# see result at http://192.168.99.100:5000/
#				192.168.99.100 -  << docker-machine ip Char >>
#				5000 - assigned port
